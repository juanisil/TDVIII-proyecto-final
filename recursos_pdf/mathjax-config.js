MathJax = {
    tex: {
        packages: { '[+]': ['color'] }, // Enable the color package
    },
    options: {
        enableMenu: false // Optional: Disable the MathJax menu for simplicity
    }
};
